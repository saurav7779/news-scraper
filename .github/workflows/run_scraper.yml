name: Run News Scraper

on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser chromium-chromedriver
      - name: Install Playwright dependencies
        run: |
          pip install playwright
          playwright install --with-deps
      - name: Install Python dependencies
        run: |
          pip install --no-cache-dir -r requirements.txt
      - name: Verify dependencies
        run: |
          python -c "import selenium; print('Selenium version:', selenium.__version__)"
          python -c "import langdetect; print('Langdetect installed')"
          python -c "import dateutil.parser; print('Dateutil.parser installed')"
          python -c "import torch; print('PyTorch version:', torch.__version__)"
          python -c "import playwright; print('Playwright installed')"
      - name: Write Firebase key to file
        env:
          FIREBASE_KEY: ${{ secrets.FIREBASE_KEY }}
        run: |
          if [ -z "$FIREBASE_KEY" ]; then echo "🚨 FIREBASE_KEY is empty or not set!"; exit 1; fi
          echo "$FIREBASE_KEY" > serviceAccountKey.json
          echo "Firebase key file created: serviceAccountKey.json"
      - name: Verify Firebase key file
        run: |
          if [ -f "serviceAccountKey.json" ]; then echo "Firebase key file exists."; else echo "🚨 Firebase key file not found!"; exit 1; fi
      - name: Run scraper
        run: |
          python scraper.py
